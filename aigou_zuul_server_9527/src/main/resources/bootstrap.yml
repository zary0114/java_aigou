spring:
  cloud:
    config:
      uri: http://127.0.0.1:1299 #配置服务器
      label: master #分支
      name: zuul_application #github上面名称
      profile: dev #环境:在达成jar包运行的时候,动态传入参数
      #profile: ${spring.actives.profile} #獲取動態運行參數
